config dnsmasq
	#option	domain 'ddmesh.de'
	#add search to resolv.conf
	#option  add_local_domain 1
	#dont add hostname/lan
	#option  add_local_hostname 0
	#same as in dnsmasq.conf.manual
	#option leasefile '/tmp/dhcp.leases'
